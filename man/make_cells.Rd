% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_cells.R
\name{make_cells}
\alias{make_cells}
\title{Assemble cell-level data into a cells object}
\usage{
make_cells(coords, embeddings, counts = NULL, meta = NULL, params = list())
}
\arguments{
\item{coords}{N x 2 numeric matrix of cell coordinates. Columns are
renamed to \code{x} and \code{y}.}

\item{embeddings}{Named list of N x D numeric matrices, one per embedding
(e.g. \code{list(pca = do_pca(counts, npcs=10)$embeddings)}).}

\item{counts}{G x N \link[Matrix:dgCMatrix-class]{dgCMatrix} of raw transcript
counts. Optional; coerced to dgCMatrix if a different sparse format is
supplied.}

\item{meta}{\link[data.table:data.table]{data.table} with N rows of per-cell
metadata. Optional; coerced from data.frame if needed.}

\item{params}{Named list of parameters carried alongside the object.}
}
\value{
A cells list with slots:
\itemize{
\item \code{coords}     — N x 2 numeric matrix
\item \code{embeddings} — named list of N x D matrices
\item \code{counts}     — G x N dgCMatrix (or NULL)
\item \code{meta}       — data.table with N rows (or NULL)
\item \code{params}     — list
}
}
\description{
Validates and structures raw inputs into the canonical cells list used
throughout the tessera pipeline. Does not compute embeddings — call
\code{\link[=do_pca]{do_pca()}} or supply pre-computed embeddings before calling this function.
}
