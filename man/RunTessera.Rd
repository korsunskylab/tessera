% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunTessera.R
\name{RunTessera}
\alias{RunTessera}
\title{Run Tessera spatial segmentation pipeline}
\usage{
RunTessera(...)
}
\arguments{
\item{...}{
  Arguments passed on to \code{\link[=RunTessera.default]{RunTessera.default}}
  \describe{
    \item{\code{meta_vars_include}}{Column names from \code{meta_data} to attach to each
surviving cell in the output \code{dmt$pts} table.}
    \item{\code{group.by}}{Column in \code{meta_data} identifying distinct samples or FOVs.
Tessera is run separately for each group. If NULL, all cells are treated
as a single group.}
    \item{\code{npcs}}{Number of PCs to compute when \code{embeddings} is NULL.}
    \item{\code{prune_thresh_quantile}}{Quantile of edge lengths above which edges are
pruned. Default 0.95.}
    \item{\code{prune_min_cells}}{Minimum cells per connected component after pruning.
Default 10.}
    \item{\code{prune_thresh}}{Absolute edge-length threshold; overrides
\code{prune_thresh_quantile} when not NA. Default NA.}
    \item{\code{smooth_distance,smooth_similarity}}{Distance and similarity weighting
for bilateral gradient smoothing. Each is one of \code{'none'}, \code{'euclidean'},
\code{'projected'}, or \code{'constant'}. Default \code{'projected'}.}
    \item{\code{smooth_iter}}{Number of bilateral smoothing iterations. Default 1.}
    \item{\code{max_npts}}{Maximum cells per tile in the main agglomeration pass.
Default 50.}
    \item{\code{min_npts}}{Minimum cells per tile in the cleanup pass. Default 5.}
    \item{\code{alpha}}{GMM sigma multiplier for transcriptional similarity scoring.
\code{0.2} = conservative merging, \code{2} = liberal merging. Default 1.}
    \item{\code{.progress}}{Show \code{furrr} progress bar. Default TRUE.}
    \item{\code{.options}}{\code{furrr_options} object passed to \code{\link[furrr:future_map]{furrr::future_map()}}.
Defaults to \code{furrr_options(stdout=TRUE, seed=TRUE)}.}
    \item{\code{future.globals.maxSize}}{Maximum size in bytes of globals exported to
parallel workers. Default 8 GB.}
    \item{\code{consolidate}}{If TRUE (default), combines results across all groups
into a single \code{list(dmt, aggs)}. If FALSE, returns a named list with one
entry per group.}
    \item{\code{verbose}}{Print per-stage progress messages. Default FALSE.}
  }}
}
\value{
See \code{\link[=RunTessera.default]{RunTessera.default()}}.
}
\description{
S3 generic that dispatches to \code{\link[=RunTessera.default]{RunTessera.default()}} for matrix/vector
inputs and \code{\link[=RunTessera.Seurat]{RunTessera.Seurat()}} for Seurat objects.
}
\seealso{
Other RunTessera: 
\code{\link{RunTessera.Seurat}()},
\code{\link{RunTessera.default}()}
}
\concept{RunTessera}
