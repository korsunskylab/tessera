% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunTessera.R
\name{RunTessera.default}
\alias{RunTessera.default}
\title{Run full Tessera segmentation pipeline on spatial transcriptomics data}
\usage{
\method{RunTessera}{default}(
  X,
  Y,
  counts = NULL,
  embeddings = NULL,
  loadings = NULL,
  meta_data = NULL,
  meta_vars_include = NULL,
  group.by = NULL,
  npcs = 10,
  prune_thresh_quantile = 0.95,
  prune_min_cells = 10,
  prune_thresh = NA,
  smooth_distance = c("none", "euclidean", "projected", "constant")[3],
  smooth_similarity = c("none", "euclidean", "projected", "constant")[3],
  smooth_iter = 1,
  max_npts = 50,
  min_npts = 5,
  alpha = 1,
  .progress = TRUE,
  .options = NULL,
  future.globals.maxSize = 8 * 1024^3,
  consolidate = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{X, Y}{Numeric vectors of length \code{num_cells} with cell coordinates.}

\item{counts}{A \code{num_genes} x \code{num_cells} gene-by-cell count matrix.
Optional if \code{embeddings} are provided.}

\item{embeddings}{A \code{num_cells} x \code{num_dim} matrix of pre-computed cell
embeddings. If NULL, PCA is computed from counts. If provided, \code{npcs}
is ignored.}

\item{loadings}{Optional \code{num_genes} x \code{num_dim} matrix of gene loadings.}

\item{meta_data}{Data frame of per-cell metadata (one row per cell).}

\item{meta_vars_include}{Column names from \code{meta_data} to attach to each
surviving cell in the output \code{dmt$pts} table.}

\item{group.by}{Column in \code{meta_data} identifying distinct samples or FOVs.
Tessera is run separately for each group. If NULL, all cells are treated
as a single group.}

\item{npcs}{Number of PCs to compute when \code{embeddings} is NULL.}

\item{prune_thresh_quantile}{Quantile of edge lengths above which edges are
pruned. Default 0.95.}

\item{prune_min_cells}{Minimum cells per connected component after pruning.
Default 10.}

\item{prune_thresh}{Absolute edge-length threshold; overrides
\code{prune_thresh_quantile} when not NA. Default NA.}

\item{smooth_distance, smooth_similarity}{Distance and similarity weighting
for bilateral gradient smoothing. Each is one of \code{'none'}, \code{'euclidean'},
\code{'projected'}, or \code{'constant'}. Default \code{'projected'}.}

\item{smooth_iter}{Number of bilateral smoothing iterations. Default 1.}

\item{max_npts}{Maximum cells per tile in the main agglomeration pass.
Default 50.}

\item{min_npts}{Minimum cells per tile in the cleanup pass. Default 5.}

\item{alpha}{GMM sigma multiplier for transcriptional similarity scoring.
\code{0.2} = conservative merging, \code{2} = liberal merging. Default 1.}

\item{.progress}{Show \code{furrr} progress bar. Default TRUE.}

\item{.options}{\code{furrr_options} object passed to \code{\link[furrr:future_map]{furrr::future_map()}}.
Defaults to \code{furrr_options(stdout=TRUE, seed=TRUE)}.}

\item{future.globals.maxSize}{Maximum size in bytes of globals exported to
parallel workers. Default 8 GB.}

\item{consolidate}{If TRUE (default), combines results across all groups
into a single \code{list(dmt, aggs)}. If FALSE, returns a named list with one
entry per group.}

\item{verbose}{Print per-stage progress messages. Default FALSE.}
}
\value{
If \code{consolidate == TRUE} (default), a list with:
\item{dmt}{Cell-level mesh data structure with slots \code{pts}, \code{tris},
\code{edges}, \code{tri_to_pt}, \code{counts}, \code{udv_cells}, \code{prim}, \code{dual}, \code{e_sep}.
\code{dmt$pts} has columns \code{X}, \code{Y}, \code{ORIG_ID}, \code{f}, \code{agg_id}, plus any
columns in \code{meta_vars_include}.}
\item{aggs}{Tile-level data structure with slots \code{meta_data}, \code{counts},
\code{pcs}, \code{edges}, \code{adj}.}

If \code{consolidate == FALSE}, a named list with one \code{list(dmt, aggs)} per
group.
}
\description{
Orchestrates four pipeline stages: (1) Delaunay mesh construction and
pruning, (2) spatial gradient field estimation with optional bilateral
smoothing, (3) Discrete Morse Theory segmentation, and (4) agglomerative
tile merging. Returns a backward-compatible output matching
\code{\link[=GetTiles.default]{GetTiles.default()}}.
}
\details{
Internally calls \code{\link[=make_cells]{make_cells()}}, \code{\link[=make_mesh]{make_mesh()}}, \code{\link[=compute_field]{compute_field()}},
\code{\link[=compute_morse]{compute_morse()}}, \code{\link[=run_dmt]{run_dmt()}}, and \code{\link[=make_tiles]{make_tiles()}}.
}
\seealso{
Other RunTessera: 
\code{\link{RunTessera}()},
\code{\link{RunTessera.Seurat}()}
}
\concept{RunTessera}
