% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_morse.R
\name{compute_morse}
\alias{compute_morse}
\title{Compute Discrete Morse Theory segmentation from gradient field and mesh}
\usage{
compute_morse(field, mesh, params = list())
}
\arguments{
\item{field}{A field list as returned by \code{\link[=compute_field]{compute_field()}}.}

\item{mesh}{A mesh list as returned by \code{\link[=make_mesh]{make_mesh()}}.}

\item{params}{Named list of DMT parameters (currently unused; reserved for
future extensions). Default \code{list()}.}
}
\value{
A morse list with slots:
\itemize{
\item \code{e_sep}  — integer vector of separatrix edge indices (1-indexed)
\item \code{prim}   — primal minimum spanning forest (from \code{\link[=do_primary_forest]{do_primary_forest()}})
\item \code{dual}   — dual maximum spanning forest (from \code{\link[=do_dual_forest]{do_dual_forest()}})
\item \code{f}      — list of scalar field values:
\itemize{
\item \code{pts}        — numeric vector length N (sum of singular values at points)
\item \code{tris}       — numeric vector length F (sum of singular values at triangles)
\item \code{edges_prim} — numeric vector length E (sum of singular values at primal edges)
\item \code{edges_dual} — numeric vector length E (sum of singular values at dual edges)
}
\item \code{params} — list() (reserved)
\item \code{meta}   — list()
}
}
\description{
Derives a scalar field from the gradient magnitude at every mesh element,
constructs a primal minimum spanning forest on points and a dual maximum
spanning forest on triangles, then traces separatrix edges that partition
the mesh into topologically coherent tiles.
}
\details{
The caller assigns the result back to the mesh:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{mesh$morse = compute_morse(field, mesh, params)
}\if{html}{\out{</div>}}

The \code{mesh$morse} slot is then read by \code{\link[=run_dmt]{run_dmt()}}.
}
\seealso{
\code{\link[=compute_field]{compute_field()}}, \code{\link[=run_dmt]{run_dmt()}}
}
