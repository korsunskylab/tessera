% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_field.R
\name{compute_field}
\alias{compute_field}
\title{Compute spatial gradient field from cell embeddings and mesh geometry}
\usage{
compute_field(cells, mesh, params = list())
}
\arguments{
\item{cells}{A cells list as returned by \code{\link[=make_cells]{make_cells()}}, with
\code{cells$embeddings$pca} set to an \code{nrow(mesh$pts)} × D matrix.}

\item{mesh}{A mesh list as returned by \code{\link[=make_mesh]{make_mesh()}}.}

\item{params}{Named list of gradient parameters:
\itemize{
\item \code{smooth_distance} — distance weighting for bilateral smoothing.
One of \code{'none'}, \code{'euclidean'}, \code{'projected'}, \code{'constant'}.
Default \code{'projected'}.
\item \code{smooth_similarity} — similarity weighting for bilateral smoothing.
One of \code{'none'}, \code{'euclidean'}, \code{'projected'}, \code{'constant'}.
Default \code{'projected'}.
\item \code{smooth_iter} — number of smoothing iterations. Default \code{1}.
}}
}
\value{
A field list with slots:
\itemize{
\item \code{gradient} — list with:
\itemize{
\item \code{pts}        — N × 6 matrix of SVD-compressed point gradients
\item \code{tris}       — F × 6 matrix of SVD-compressed triangle gradients
\item \code{edges_pts}  — E × 6 matrix of SVD-compressed primal edge gradients
\item \code{edges_tris} — E × 6 matrix of SVD-compressed dual edge gradients
\item \code{params}     — list of smoothing parameters used
\item \code{meta}       — list()
}

All SVD matrices have columns:
\code{dx_grad}, \code{dy_grad}, \code{dx_ortho}, \code{dy_ortho}, \code{len_grad}, \code{len_ortho}.
\item \code{meta} — list()
}
}
\description{
Estimates a spatial gradient at every mesh element (points, triangles,
edges) from cell embeddings, applies optional bilateral smoothing, and
returns SVD-compressed gradient representations used by \code{\link[=compute_morse]{compute_morse()}}.
}
\details{
\strong{Important:} \code{cells$embeddings$pca} must contain exactly \code{nrow(mesh$pts)}
rows — one per cell that survived pruning in \code{\link[=make_mesh]{make_mesh()}}. Compute
embeddings on the pruned cells after \code{\link[=make_mesh]{make_mesh()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{mesh             = make_mesh(cells, params)
pca              = do_pca(cells$counts[, mesh$pts$ORIG_ID], npcs = 10)
cells$embeddings = list(pca = pca$embeddings)
field            = compute_field(cells, mesh, params)
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=make_mesh]{make_mesh()}}, \code{\link[=compute_morse]{compute_morse()}}
}
