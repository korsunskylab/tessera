% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_mesh.R
\name{make_mesh}
\alias{make_mesh}
\title{Construct Delaunay triangulation mesh from cells}
\usage{
make_mesh(cells, params = list())
}
\arguments{
\item{cells}{A cells list as returned by \code{\link[=make_cells]{make_cells()}}.}

\item{params}{Named list of mesh-construction parameters:
\itemize{
\item \code{prune_thresh_quantile} — quantile of edge length above which edges
are pruned (default 0.95).
\item \code{prune_mincells} — minimum cells required to keep a connected component
(default 10).
\item \code{prune_thresh} — absolute edge-length threshold; overrides
\code{prune_thresh_quantile} when not \code{NA} (default NA).
}}
}
\value{
A mesh list with slots:
\itemize{
\item \code{pts}       — data.table of surviving cells (columns X, Y, ORIG_ID, ...)
\item \code{edges}     — data.table of mesh edges (from_pt, to_pt, from_tri, to_tri, ...)
\item \code{triangles} — data.table of triangles (X, Y, area, height, external)
\item \code{tri_to_pt} — dgCMatrix (num_tris × num_pts): 1 where triangle uses point
\item \code{adj}       — dgCMatrix (num_pts × num_pts): symmetric point adjacency
\item \code{weights}   — numeric vector length num_edges (all 1, reserved for future use)
\item \code{morse}     — NULL until populated by \code{\link[=compute_morse]{compute_morse()}}
\item \code{meta}      — list()
}
}
\description{
Builds a pruned Delaunay triangulation over cell coordinates, adds
degenerate exterior triangles along the boundary, and pre-computes
the sparse point adjacency matrix used by subsequent pipeline stages.
}
\details{
Pruning has two steps: (1) edges longer than a length threshold are
removed along with any triangles that contain them, and (2) connected
components with fewer than \code{prune_mincells} cells are removed.
}
\seealso{
\code{\link[=make_cells]{make_cells()}}, \code{\link[=compute_field]{compute_field()}}
}
