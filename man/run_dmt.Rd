% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_dmt.R
\name{run_dmt}
\alias{run_dmt}
\title{Assign cells to initial DMT tiles using separatrix edges}
\usage{
run_dmt(mesh, params = list())
}
\arguments{
\item{mesh}{A mesh list as returned by \code{\link[=make_mesh]{make_mesh()}}, with \code{mesh$morse}
populated by \code{\link[=compute_morse]{compute_morse()}}.}

\item{params}{Named list of DMT parameters (currently unused; reserved for
future extensions). Default \code{list()}.}
}
\value{
A dmt list with slots:
\itemize{
\item \code{pts}   — data.table copy of \code{mesh$pts} with an added \code{agg_id} integer
column giving each cell's initial tile assignment.
\item \code{edges} — data.table copy of \code{mesh$edges} with added \code{agg_from} and
\code{agg_to} integer columns giving the tile IDs of each edge's endpoints.
\item \code{meta}  — list()
}
}
\description{
Removes the separatrix edges stored in \code{mesh$morse$e_sep} from the mesh
graph and labels each connected component as a distinct tile.  The result
is used as input to \code{\link[=make_tiles]{make_tiles()}}, which performs the agglomerative
merging step.
}
\details{
Typical usage:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{field      = compute_field(cells, mesh)
mesh$morse = compute_morse(field, mesh)
dmt        = run_dmt(mesh)
tiles      = make_tiles(cells, mesh, dmt, params)
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=compute_morse]{compute_morse()}}, \code{\link[=make_tiles]{make_tiles()}}
}
