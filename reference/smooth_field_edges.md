# Bilateral / anisotropic filtering of gradient field

Bilateral / anisotropic filtering of gradient field

## Usage

``` r
smooth_field_edges(
  edges,
  field,
  edges_svd,
  coords,
  adj_idx,
  distance = "projected",
  similarity = "euclidean"
)
```
